filter-map main(bla: Bla) {
    apply {
        if bla.y() > 10 {
            accept 2 * bla.y()
        } else {
            reject
        }
    }
}

filter-map just_reject(x: u32) {
    apply { reject }
}
